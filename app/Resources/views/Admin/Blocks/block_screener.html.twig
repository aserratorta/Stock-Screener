{% extends sonata_block.templates.block_base %}

{% block block %}
        <div class="box">
            <div class="box-header">
                <h3 class="box-title">
                    <i class="fa fa-envelope-o"></i>
                        {{ title }}
                </h3>
            </div>
            <div class="box-body">
                <a class="btn btn-default" href="{{ path('admin_app_screener_screener') }}">Botó</a>
            </div>
        </div>
{% endblock %}
