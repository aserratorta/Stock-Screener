{% extends sonata_block.templates.block_base %}

{% block block %}
        <div class="box">
            <div class="box-header">
                <h3 class="box-title">
                    <i class="fa fa-line-chart" aria-hidden="true"></i>
                        {{ title }}
                </h3>
            </div>
            <div class="box-body">
                <a type="button" class="btn btn-primary" href="{{ path('admin_app_screener_filter') }}">Botó</a>
            </div>
        </div>
{% endblock %}
